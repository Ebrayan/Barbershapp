<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thebryan.combarbershapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Services_List" />
        <activity android:name=".Reservation" />
        <activity android:name=".Store" />
        <activity android:name=".Turns" />
        <activity
            android:name=".Turn"
            android:label="@string/title_activity_turn"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".ViewImage" />
        <activity android:name=".NewService" />
        <activity android:name=".Portafolio" />
        <activity
            android:name=".MyReservatios"
            android:label="@string/title_activity_my_reservatios"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
    </application>

</manifest>